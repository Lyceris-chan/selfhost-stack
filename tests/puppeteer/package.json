{
  "name": "selfhost-stack-puppeteer-tests",
  "private": true,
  "version": "0.1.0",
  "description": "Local Puppeteer checks for the privacy hub stack.",
  "scripts": {
    "test": "node run-tests.js",
    "test:puppeteer": "node run-tests.js"
  },
  "dependencies": {
    "puppeteer": "^22.10.0"
  }
}
