shield shouldnt be in its own chip within the service chip as its increasing the height of the services chips make it a small icon on the service card that wont do that
admin UI elements still show during reload this should be made to be impossible
Update the hover descriptions on these System Core VPN Protected Direct Access 

cobalt is showing to be version 11.2 which isnt the latest version

its showing updates are available right after deployment

Unbound and VPN Gateway and Watchtower and showing connecting... on the dashboard

the dashboard is still saying im using a self signed certificate which isnt the case

immich is still saying container ID unavailable for the manage in portainer button

the logs section is still saying 
Connecting to Log Stream...

the storage breakdown ui has 2 lines (divs?) due to the hidden reclaimable storage and make it so the Prune unused images button text says Prune unused assets instead 

ensure text isnt cut off for things like e.g Secure DOH (browsers)

updates available banner is visible upon reload even after dismissing it 

mac address banner doesnt show / dismiss reliably

the admin login button disappears in admin mode making it so the admin cant logout / destroy that session

icons / UI elements are still broken e.g devices, vpn_key etc

wg-easy profile name doesnt appear properly centered in the QR screen

odido integration results in the following error checkout this repo for reference and clone it and properly investigate what our problem is 
https://github.com/GuusBackup/Odido.Authenticator

Failed: HTTPSConnectionPool(host='login.odido.nl', port=443): Max retries exceeded with url: /connect/token (Caused by NameResolutionError("HTTPSConnection(host='login.odido.nl', port=443): Failed to resolve 'login.odido.nl' ([Errno -5] Name has no usable address)")) 

